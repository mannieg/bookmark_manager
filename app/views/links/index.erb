<h1> Links </h1>

<a href="links/new" class="button" >Add New Link</a>

  <ul id="links">
    <%@links.each do |link|%>
      <li>
        Title: <%= link.title %>
        URL: <%= link.url %>
        Tags:  <%= link.tags.map(&:name).join %>
      </li>
    <% end %>
  </ul>
